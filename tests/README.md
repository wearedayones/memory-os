# tests/

Use this directory for unit and integration tests for hermes-memory.

- `tests/unit/` - fast tests for individual modules.
- `tests/integration/` - tests that touch JSON files, Git workflow, and retrieval end-to-end.

This scaffold intentionally contains no Python test files yet. Future subagents should add pytest-based tests here.
